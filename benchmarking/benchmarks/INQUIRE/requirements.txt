# INQUIRE Benchmark Requirements
# Core benchmarking framework
# TODO: Pin to a specific version once you have the eval working
# imsearch_eval[weaviate] @ git+https://github.com/waggle-sensor/imsearch_eval.git@0.1.7
imsearch_eval[weaviate] @ git+https://github.com/waggle-sensor/imsearch_eval.git
# Dataset loading
datasets>=2.14.0
huggingface-hub>=0.16.0

# Image processing
Pillow>=10.0.0

# Date parsing
python-dateutil>=2.8.0
