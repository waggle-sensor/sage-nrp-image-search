# CommonObjectsBench Benchmark Requirements

# weaviate-client import checks protobuf vs grpcio; cap protobuf 5.x so pip does not
# pull 6.x (WeaviateProtobufIncompatibility with grpcio 1.67.x).
protobuf>=5.26.1,<6

# Core benchmarking framework (pre-release: milvus branch; switch to @0.2.0 after release)
imsearch_eval[weaviate] @ git+https://github.com/waggle-sensor/imsearch_eval.git@milvus
imsearch_eval[milvus] @ git+https://github.com/waggle-sensor/imsearch_eval.git@milvus
imsearch_eval[triton] @ git+https://github.com/waggle-sensor/imsearch_eval.git@milvus
imsearch_eval[huggingface] @ git+https://github.com/waggle-sensor/imsearch_eval.git@milvus
imsearch_eval[nrp] @ git+https://github.com/waggle-sensor/imsearch_eval.git@milvus

# Match Weaviate server 1.32.x (Python client 4.16.x per compatibility matrix).
# https://docs.weaviate.io/weaviate/client-libraries/python
weaviate-client>=4.16.0,<4.17.0

# Image processing
Pillow>=10.0.0

# Date parsing
python-dateutil>=2.8.0

# S3 upload support (MinIO)
minio>=7.2.0

# Plotting
matplotlib